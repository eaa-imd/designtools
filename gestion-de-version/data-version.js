// On définit les choses:

var dataVersion = [
  [ 'github', 10  ],
  [ 'Google drive',   7  ],
	[ 'Bitbucket',  2 ],
	[ 'Dropbox',  2 ],
	[ 'Abstract',  1 ],
];

var labelsVersion = arrayKeys(dataVersion);
var seriesVersion = arrayValues(dataVersion);

// avec arrayKeys() on obtient les labels
// avec arrayValues() on obtient les chiffres
